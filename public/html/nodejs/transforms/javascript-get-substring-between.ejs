<!DOCTYPE html>
<html>
<head>
	<% include ../../partials/bootstraplinks %>
	<link rel="stylesheet" type="text/css" href="/styles/codeSnippets.css">
	<!-- Standard snippet title. This will show up as the anchor text to this snippet, so please customize -->
	<title>
		Get substring between two unique strings with Javascript
	</title>
</head>
<body>
	<% include ../../partials/navbar %>
	<div class="container">
		<div class="col-md-10 container container-top">
			<!-- Enter a description for your code here -->
			<p>
				This is a bit of an edge case. I needed to access the contents of an HTML node for this app. Generally, you'd waid until the DOM is loaded and do that, but I couldn't. So, I made sure that the node name was unique, and used Javascript to match the strings. The following function takes a string (the read document), and returns the match.
			</p>
		</div>
		<div class="col-lg-10 container">
			<pre class="prettyprint linenums">
				<!-- Place your code here. The pre tag preserves whitespace, linebreaks, etc..., so make it pretty! -->
				<code>
					function getNodeContent(code) {
						var first = &quot;&lt;myNode&gt;&quot;;
						var second = &quot;&lt;&#x2F;myNode&gt;&quot;
						return code.substring(code.lastIndexOf(first)+first.length,code.lastIndexOf(second));
					}
				</code>
			</pre>
		</div>
	</div>
</body>
</html>